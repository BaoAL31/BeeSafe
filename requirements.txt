# MCUNet: clone to modeling/mcunet (mit-han-lab/mcunet) then:
#   pip install -e ./modeling/mcunet
-e ./modeling/mcunet
torch
torchvision
tensorboard>=2.14.0
# setuptools 82+ drops top-level pkg_resources; TensorBoard still imports it.
setuptools>=65,<82
Pillow>=9.0.0
psutil>=5.9.0
pynvml>=11.5.0
pyyaml>=6.0

# Audio processing dependencies
# torchaudio  # uncomment if version matches torch (2.5.1 needs torchaudio>=2.5.0)
librosa>=0.10.0
soundfile>=0.12.0
scipy>=1.10.0
requests>=2.28.0
ultralytics>=8.4.45
numpy>=2.1.3
scikit-learn>=1.6.1
